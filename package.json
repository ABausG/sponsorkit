{
    "comment": "The package.json is used to configure sponsorkit and nothing else",
    "private": true,
    "scripts": {
        "build": "sponsorkit"
    },
    "devDependencies": {
        "sponsorkit": "latest"
    },
    "dependencies": {
        "sharp": "^0.33.4",
        "yarn": "^1.22.22"
    }
}
